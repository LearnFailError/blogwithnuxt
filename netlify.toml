[build]
  command = "npm run build"
  publish = ".output/public"

[dev]
  command = "npm run dev"
  port = 3000
  publish = "dist"

# Redirect untuk SPA (Single Page Application)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200